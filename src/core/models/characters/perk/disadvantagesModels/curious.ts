import { disadvantageList } from '../type';

const dataStructure = `
Объект
  Преимущества
    Любопытсов
      Уровень: 0
`

export const Curious: { code: disadvantageList, rawStruct: string } = { code: 'curious', rawStruct: dataStructure };