export default function getCharacter(input: any) {
  const { data } = input;
  const { name } = data;
  // const character = new Character({
  //   name,
  // });
  //return character;
  return '';
}
